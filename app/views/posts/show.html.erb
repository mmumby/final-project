<p id="notice"><%= notice %></p>

<p>
  <strong>Description:</strong>
  <%= @post.description %>
</p>

<p>
  <strong>Image:</strong>
  <%= @post.latitude %>
</p>

<p>
  <strong>Location:</strong>
  <img src="https://maps.googleapis.com/maps/api/staticmap?center=<%= @post.latitude %>,<%= @post.longitude %>&zoom=15&size=640x426&scale=1&key=#{ENV['GOOGLE_STATIC_MAP_KEY']}">
</p>

<h2>Comments</h2>
<%= render 'comments/comments' %>
<%= render 'comments/form' %>
<%= link_to 'Chat', edit_post_path(@post) %> |
<%= link_to 'Back', posts_path %>
